<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jbp.service.dao.UserScoreDao">
    <select id="getList" resultType="com.jbp.common.model.user.UserScore">
        select *,u.nickname,u.phone,u.account,t.name as team_name
        from eb_user_score s
        left join eb_user u on s.uid = u.id
        left join eb_team_user tu on s.uid = tu.uid
        left join eb_team t on tu.tid = t.id
        <where>
            <if test="uid !=null and uid!=''">
                and s.uid=#{uid}
            </if>
            <if test="nickname !=null and nickname!=''">
                and u.nickname like CONCAT('%',#{nickname},'%')
            </if>
            <if test="phone !=null and phone!=''">
                and u.phone=#{phone}
            </if>
        </where>
        order by s.id desc
    </select>
</mapper>
