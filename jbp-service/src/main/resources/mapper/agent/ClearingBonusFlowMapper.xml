<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jbp.service.dao.agent.ClearingBonusFlowDao">
    <select id="getClearingInfoList" resultType="com.jbp.common.response.ClearingBonusListResponse">
        select  commAmt, DATE_FORMAT(startTime, '%Y-%m-%d') as time, levelName, e.postscript
        from eb_clearing_bonus_flow e
            left join eb_clearing_final f
        on f.id = e.clearingId
        where e.uid = #{uid} and DATE_FORMAT( startTime , '%Y-%m-%d') = #{day}
        order by e.level
    </select>
</mapper>
